import { Meta } from "@storybook/addon-docs";
import { defaultDocParams } from "../../base/lib/utils";
import CodeView from "../../react/base/components/CodeView";

import StateJsContent from "!raw-loader!../../dist/vanilla/stories/base/lib/VanillaStore";
import StateTsContent from "!raw-loader!./base/lib/VanillaStore";

import BasicUsageJsContent from "!raw-loader!../../dist/vanilla/stories/base/components/VanillaBasicUsage";
import BasicUsageTsContent from "!raw-loader!./base/components/VanillaBasicUsage";

<Meta title="Vanilla/Basic usage" parameters={defaultDocParams()} />

## Installation

### npm

```shell
npm i react-gstatem
```

### yarn

```shell
yarn add react-gstatem
```

## Vanilla - basic usage

**Create a store**

The exported functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

<CodeView
	componentName="VanillaStore"
	jsContent={StateJsContent}
	tsContent={StateTsContent}
/>

**Use the store in pure javascript**

<CodeView
	componentName="VanillaBasicUsage"
	jsContent={BasicUsageJsContent}
	tsContent={BasicUsageTsContent}
/>
