import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultDocParams } from "../../../base/lib/utils";
import CodeView from "../../../base/components/CodeView";

import Installation from "../../../base/components/Installation.mdx";

import VueJSXMultiStores from "./VueJSXMultiStores.tsx";
import Store1JsContent from "!raw-loader!../../../dist/vue/stories/VueMultiStores/VueMultiStore1";
import Store1TsContent from "!raw-loader!../VueMultiStores/VueMultiStore1";

import Store2JsContent from "!raw-loader!../../../dist/vue/stories/VueMultiStores/VueMultiStore2";
import Store2TsContent from "!raw-loader!../VueMultiStores/VueMultiStore2";

import ComponentJsContent from "!raw-loader!../../../dist/vue/stories/VueJSXMultiStores/VueJSXMultiStores";
import ComponentTsContent from "!raw-loader!./VueJSXMultiStores";

<Meta title="Vue/JSX/Multiple stores" parameters={defaultDocParams()} />

<Installation packages="vue-gstatem" />

## Vue JSX - Multiple stores

**Rendered view**

<Canvas className="custom-docs-story">
	<Story name="Multiple stores">
		{{
			components: { VueJSXMultiStores },
			template: "<VueJSXMultiStores />"
		}}
	</Story>
</Canvas>

### Recipes

---

The `increaseCount*` functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

**Create store 1**

<CodeView
	componentName="VueMultiStore1"
	jsContent={Store1JsContent}
	tsContent={Store1TsContent}
/>

**Create store 2**

<CodeView
	componentName="VueMultiStore2"
	jsContent={Store2JsContent}
	tsContent={Store2TsContent}
/>

**Use the store in component**

<CodeView
	componentName="VueJSXMultiStores"
	jsContent={ComponentJsContent}
	tsContent={ComponentTsContent}
/>
