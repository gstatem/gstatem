import { Meta, Canvas, Story } from "@storybook/addon-docs";
import { defaultDocParams } from "../../../base/lib/utils";
import CodeView from "../../../base/components/CodeView";

import Installation from "../../../base/components/Installation.mdx";

import VueJSXBasicUsage from "./VueJSXBasicUsage.tsx";
import StoreJsContent from "!raw-loader!../../../dist/vue/stories/VueBasicUsage/VueStore";
import StoreTsContent from "!raw-loader!../VueBasicUsage/VueStore";

import ComponentJsContent from "!raw-loader!../../../dist/vue/stories/VueJSXBasicUsage/VueJSXBasicUsage";
import ComponentTsContent from "!raw-loader!./VueJSXBasicUsage";

<Meta title="Vue/JSX/Basic usage" parameters={defaultDocParams()} />

<Installation packages="vue-gstatem" />

## Vue JSX - Basic usage

**Rendered view**

<Canvas className="custom-docs-story">
	<Story name="Basic usage">
		{{
			components: { VueJSXBasicUsage },
			template: "<VueJSXBasicUsage />"
		}}
	</Story>
</Canvas>

### Recipes

---

**Create a store**

The `increaseCount` and `resetCount` functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

<CodeView
	componentName="VueStore"
	jsContent={StoreJsContent}
	tsContent={StoreTsContent}
/>

**Use the store in component**

<CodeView
	componentName="VueJSXBasicUsage"
	jsContent={ComponentJsContent}
	tsContent={ComponentTsContent}
/>
