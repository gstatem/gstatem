import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultDocParams } from "../../../base/lib/utils";
import CodeView from "../../../base/components/CodeView";

import Installation from "../../../base/components/Installation.mdx";

import VueCustomEqual from "./VueCustomEqual";
import StoreJsContent from "!raw-loader!../../../dist/vue/stories/VueCustomEqual/VueStoreCustomEqual";
import StoreTsContent from "!raw-loader!./VueStoreCustomEqual";

import ComponentContent from "!raw-loader!../../../dist/vue/stories/VueCustomEqual/VueCustomEqual.txt";

<Meta title="Vue/Custom equal" parameters={defaultDocParams()} />

<Installation packages="vue-gstatem" />

## Vue - Custom equal

**Rendered view**

The component will NOT be re-rendered if the diff between current count and next count is less or equal to 1.

<Canvas className="custom-docs-story">
	<Story name="Custom equal">
		{{
			components: { VueCustomEqual },
			template: "<VueCustomEqual />"
		}}
	</Story>
</Canvas>

### Recipes

---

**Create a store**

The `increaseCount` and `resetCount` functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

<CodeView
	componentName="VueStoreCustomEqual"
	jsContent={StoreJsContent}
	tsContent={StoreTsContent}
/>

**Use the store in component**

<CodeView
	componentName="VueCustomEqual"
	lang="vue"
	vueContent={ComponentContent}
/>
