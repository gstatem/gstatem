import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultDocParams } from "../../../base/lib/utils";
import CodeView from "../../../base/components/CodeView";

import Installation from "../../../base/components/Installation.mdx";

import VueSilentMode from "./VueSilentMode";
import StoreJsContent from "!raw-loader!../../../dist/vue/stories/VueSilentMode/VueStoreSilentMode";
import StoreTsContent from "!raw-loader!./VueStoreSilentMode";

import ComponentContent from "!raw-loader!../../../dist/vue/stories/VueSilentMode/VueSilentMode.txt";

<Meta title="Vue/Silent mode" parameters={defaultDocParams()} />

<Installation packages="vue-gstatem" />

## Vue - Silent mode

**Rendered view**

The `+` button will NOT trigger component re-render.

<Canvas className="custom-docs-story">
	<Story name="Silent mode">
		{{
			components: { VueSilentMode },
			template: "<VueSilentMode />"
		}}
	</Story>
</Canvas>

### Recipes

---

**Create a store**

The exported functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

Please use `get` and `set` functions with care as they won't trigger component re-render.

<CodeView
	componentName="VueStoreSilentMode"
	jsContent={StoreJsContent}
	tsContent={StoreTsContent}
/>

**Use the store in component**

<CodeView
	componentName="VueSilentMode"
	lang="vue"
	vueContent={ComponentContent}
/>
