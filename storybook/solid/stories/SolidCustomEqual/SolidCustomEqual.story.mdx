import { Meta } from "@storybook/addon-docs";
import { defaultDocParams } from "../../../base/lib/utils";
import CodeView from "../../../base/components/CodeView";

import Installation from "../../../base/components/Installation.mdx";

import StoreJsContent from "!raw-loader!../../../dist/solid/stories/SolidCustomEqual/SolidStoreCustomEqual";
import StoreTsContent from "!raw-loader!./SolidStoreCustomEqual";

import ComponentJsContent from "!raw-loader!../../../dist/solid/stories/SolidCustomEqual/SolidCustomEqual";
import ComponentTsContent from "!raw-loader!./SolidCustomEqual";

<Meta title="Solid/Custom equal" parameters={defaultDocParams()} />

<Installation packages="solid-gstatem" />

## Solid - Custom equal

**Create a store**

The `increaseCount` and `resetCount` functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

<CodeView
	componentName="SolidStoreCustomEqual"
	jsContent={StoreJsContent}
	tsContent={StoreTsContent}
/>

**Use the store in component**

<CodeView
	componentName="SolidCustomEqual"
	jsContent={ComponentJsContent}
	tsContent={ComponentTsContent}
/>
