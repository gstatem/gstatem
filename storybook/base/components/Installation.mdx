import { Source } from "@storybook/addon-docs";

## Installation

### npm

<Source
	dark={true}
	language="shell"
	code={`npm i ${props.packages || "react-gstatem"}`}
/>

### yarn

<Source
	dark={true}
	language="shell"
	code={`npm i ${props.packages || "react-gstatem"}`}
/>
