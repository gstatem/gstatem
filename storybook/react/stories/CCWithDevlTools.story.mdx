import { Canvas, Meta } from "@storybook/addon-docs";
import { defaultDocParams } from "../../base/lib/utils";
import CodeView from "../base/components/CodeView";

import Installation from "../../base/components/Installation.mdx";

import CCWithDevTools from "./base/components/CCWithDevTools";
import StoreJsContent from "!raw-loader!../../dist/react/stories/base/stores/CCStoreWithDevTools";
import StoreTsContent from "!raw-loader!./base/stores/CCStoreWithDevTools";

import ComponentJsContent from "!raw-loader!../../dist/react/stories/base/components/CCWithDevTools";
import ComponentTsContent from "!raw-loader!./base/components/CCWithDevTools";

import DevToolsImage from "../../base/assets/devtools.gif";

<Meta
	title="React/Class component/With devtools"
	parameters={defaultDocParams()}
/>

<Installation packages="react-gstatem gstatem-devtools" />

## Class component - with devtools

**Rendered view**

<Canvas className="custom-docs-story">
	<CCWithDevTools />
</Canvas>

### Recipes

---

**Create a store**

The exported functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

<CodeView
	componentName="CCStoreWithDevTools"
	jsContent={StoreJsContent}
	tsContent={StoreTsContent}
/>

**Use the store in component**

<CodeView
	componentName="CCWithDevTools"
	jsContent={ComponentJsContent}
	tsContent={ComponentTsContent}
/>

Every dispatched piece will be logged in the Chrome extension [GStatem-devtools](https://chrome.google.com/webstore/detail/gstatem-devtools/djohekcenmdagbolgaiiphdnmhgmpllk) if the devtools is installed.

<img className="image-border" src={DevToolsImage} alt="devtools" />
