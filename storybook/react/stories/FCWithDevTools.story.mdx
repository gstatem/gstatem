import { Canvas, Meta } from "@storybook/addon-docs";
import { defaultDocParams } from "../../base/lib/utils";
import CodeView from "../base/components/CodeView";

import FCWithDevTools from "./base/components/FCWithDevTools";
import StoreJsContent from "!raw-loader!../../dist/react/stories/base/stores/FCStoreWithDevTools";
import StoreTsContent from "!raw-loader!./base/stores/FCStoreWithDevTools";

import ComponentJsContent from "!raw-loader!../../dist/react/stories/base/components/FCWithDevTools";
import ComponentTsContent from "!raw-loader!./base/components/FCWithDevTools";

import DevToolsImage from "../../base/assets/devtools.gif";

<Meta
	title="React/Function component/With devtools"
	parameters={defaultDocParams()}
/>

## Installation

### npm

```shell
npm i react-gstatem gstatem-devtools
```

### yarn

```shell
yarn add react-gstatem gstatem-devtools
```

## Function component - with devtools

**Rendered view**

<Canvas className="custom-docs-story">
	<FCWithDevTools />
</Canvas>

### Recipes

---

**Create a store**

The `increaseCount` and `resetCount` functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

<CodeView
	componentName="FCStoreWithDevTools"
	jsContent={StoreJsContent}
	tsContent={StoreTsContent}
/>

**Use the store in component**

<CodeView
	componentName="FCWithDevTools"
	jsContent={ComponentJsContent}
	tsContent={ComponentTsContent}
/>

Every dispatched piece will be logged in the Chrome extension [GStatem-devtools](https://chrome.google.com/webstore/detail/gstatem-devtools/djohekcenmdagbolgaiiphdnmhgmpllk) if the devtools is installed.

<img className="image-border" src={DevToolsImage} alt="devtools" />
