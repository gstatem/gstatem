import { Canvas, Meta } from "@storybook/addon-docs";
import { defaultDocParams } from "../../base/lib/utils";
import CodeView from "../../base/components/CodeView";

import Installation from "../../base/components/Installation.mdx";

import FCCustomEqual from "./base/components/FCCustomEqual";
import StoreJsContent from "!raw-loader!../../dist/react/stories/base/stores/FCStoreCustomEqual";
import StoreTsContent from "!raw-loader!./base/stores/FCStoreCustomEqual";

import ComponentJsContent from "!raw-loader!../../dist/react/stories/base/components/FCCustomEqual";
import ComponentTsContent from "!raw-loader!./base/components/FCCustomEqual";

<Meta
	title="React/Function component/Custom equal"
	parameters={defaultDocParams()}
/>

<Installation />

## Function component - custom equal

**Rendered view**

The component will NOT be re-rendered if the diff between current count and next count is less or equal to 1.

<Canvas className="custom-docs-story">
	<FCCustomEqual />
</Canvas>

### Recipes

---

**Create a store**

The `increaseCount` and `resetCount` functions can be used anywhere - in component, utils file, event listener, setTimeout, setInterval and promise callbacks.

<CodeView
	componentName="FCStore"
	jsContent={StoreJsContent}
	tsContent={StoreTsContent}
/>

**Use the store in component**

<CodeView
	componentName="FCBasicUsage"
	jsContent={ComponentJsContent}
	tsContent={ComponentTsContent}
/>
